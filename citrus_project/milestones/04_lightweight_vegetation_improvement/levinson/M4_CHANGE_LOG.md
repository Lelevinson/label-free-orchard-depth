# Milestone 4 (Levinson) Change Log

Dated provenance log for Levinson's Milestone 4 workstream, moved out of the top-level
`AGENTS.md` to keep that file at pointer + current-status altitude (doc-layering principle).

This is a chronological provenance record. For current status and the current lead method,
use `AGENTS.md`, this folder's `README.md`, `snapshots/README.md`, and `DOCUMENTATION_INDEX.md`.
Project-level (pre-Milestone-4) history remains in the `AGENTS.md` change log.

1. 2026-05-13: Added folder-level README maps and migrated B0 inference weights, command scripts, no-code-changes marker, result files, visual panels, and `opt.json` into Levinson's agreed Milestone 4 snapshot structure under `levinson/snapshots/00_plain_citrus_baseline/`; removed the old `baseline_checkpoint/` copy.
2. 2026-05-13: Added Milestone 4 workstream folders for `levinson/` and `Marvel/`, moved the Milestone 4 `results/` and local ignored `runs/` folders under `levinson/`, and recorded the rule that tested `.py` improvements must be duplicated into the matching stage snapshot.
3. 2026-05-14: Implemented disabled-by-default photometric-confidence masking in `options.py` and `trainer.py`, ran CUDA smoke plus a 250-step self-supervised gate and same-budget no-mask control, saved visual comparison panels, and packaged Levinson snapshot `01_photometric_confidence_masking/` with an `uncertain / do not scale yet` conclusion.
4. 2026-05-14: Ran the approved overnight Milestone 4 self-supervised queue: `02_rgb_edge_structure_preserving_loss` first, then independent backup `03_soft_confidence_multiplier` because 02 was negative. Both were stable 250-step gates but worsened median-scaled validation metrics versus the same-budget no-mask control, so both snapshots were packaged with `stop` conclusions and no 04 vegetation-aware training was run.
5. 2026-05-14: Restored the live root `options.py` and `trainer.py` to the shared baseline state after packaging the 01/02/03 snapshot code copies, reducing collaboration friction while preserving reproducibility evidence in Levinson's snapshots.
6. 2026-05-14: Recorded Milestone 4 workstream separation: Levinson owns the self-supervised RGB-only training path, while Marvel can explore supervised/hybrid valid-depth, valid-mask, or LiDAR-guided training in a separate workstream with honest labeling.
7. 2026-05-17: Implemented and tested Levinson snapshot `04_vegetation_general_temporal_cross_view_consistency/` with temporal geometry, visibility, texture ambiguity, feature consistency, and full ablations. The source-frame branch was revised to a stop-gradient teacher for practical batch-size-12 gates. Results are stable weak negative evidence; do not scale Snapshot 04. Root `options.py`, `trainer.py`, and the new diagnostic renderer remain as the active method branch, with tested copies and `patches/final_method.diff` saved in the snapshot.
8. 2026-05-17: Added the Snapshot 04 diagnostic report, then superseded its design-only Snapshot 05 proposal with the completed `05_teacher_anchored_relative_structure_regularization/` full 30-epoch run. Snapshot 05 uses a frozen RGB-only Lite-Mono teacher for scale-invariant structure, gradient, and ranking regularization with no Citrus labels or valid masks as training losses. Final val/test metrics are promising mixed evidence: better than B0 on raw and median-scaled `abs_rel`, close to B0 on median-scaled `a1`, better than original on median-scaled `a1`, but still worse than original on median-scaled `abs_rel`. Root code remains active as the Snapshot 05 branch, with tested copies and `patches/final_method.diff` saved in the snapshot.
9. 2026-05-18: Completed Levinson snapshot `06_teacher_anchor_stabilization/`, a config-only stabilization of Snapshot 05 with reduced ranking weight (`0.005`) and no texture-ambiguity emphasis. The full 30-epoch run is stable and packaged; validation is slightly better than Snapshot 05 but test is slightly worse, so Snapshot 06 is a promising mixed/marginal ablation rather than a clean replacement. Root code remains the active teacher-anchored branch.
10. 2026-05-18: Ran validation-first checkpoint selection for Snapshot 05 and Snapshot 06 without new training. The selected checkpoints are Snapshot 05 `weights_19` and Snapshot 06 `weights_25`; test evaluation after selection shows Snapshot 05 `weights_19` is strongest so far, with test median-scaled `abs_rel=0.3947` and `a1=0.6476`.
11. 2026-05-19: Packaged Snapshot 05 selected `weights_19` as the then-best Levinson label-free teacher-anchor candidate without new training. Generated validation/test comparison panels versus original Lite-Mono, B0, and Snapshot 05 `weights_29`, plus plain selected-checkpoint RGB/depth/disparity outputs under `citrus_project/milestones/04_lightweight_vegetation_improvement/levinson/snapshots/05_teacher_anchored_relative_structure_regularization/local_evidence/selected_weights19_visuals/`. Visuals are mixed but support using `weights_19` as the main Snapshot 05 comparison point with the caveat that it still does not beat original Lite-Mono on median-scaled `abs_rel`.
12. 2026-05-19: Tidied large Levinson generated evidence into snapshot-local `local_evidence/` folders and the checkpoint-selection note's `local_results/` folder. Added local-only `.git/info/exclude` rules for those bulky generated folders and for future `levinson/results/` outputs, and set this repository's local `core.excludesfile` to `.git/info/exclude`, so the user's checkout can keep evidence without flooding `git status`.
13. 2026-05-19: Generated a professor-facing Snapshot 05 `weights_19` visual diagnosis package without new training at `citrus_project/milestones/04_lightweight_vegetation_improvement/levinson/snapshots/05_teacher_anchored_relative_structure_regularization/local_evidence/selected_weights19_professor_visual_diagnostics/`. The package separates full-image qualitative issues from valid-LiDAR evaluation behavior and concludes that `weights_19` is numerically promising and professor-discussion-ready, but not visually solved or paper-polished.
14. 2026-05-19: Added the pre-Snapshot07 repository audit now archived at `citrus_project/milestones/04_lightweight_vegetation_improvement/levinson/archive/research_process_notes/pre_snapshot07_repo_audit.md`, documented that root `options.py` and `trainer.py` remained active as the Snapshot 05/06 teacher-anchor branch before Snapshot 07, and promoted the local-evidence ignore policy into shared `.gitignore` while keeping `.git/info/exclude` as a personal safety net.
15. 2026-05-19: Added `citrus_project/milestones/04_lightweight_vegetation_improvement/levinson/ACTIVE_ROOT_CODE_STATE.md` to make the active-root policy explicit before Snapshot 07: root code is the active workbench used by `train.py`, snapshot `code/` folders are frozen archival copies, and future methods must inspect and document root-code state before editing or training.
16. 2026-05-20: Completed and packaged Levinson snapshot `07_structure_aware_label_free_vegetation_depth/`. Snapshot 07 adds reliable-boundary teacher weighting and RGB-only sky/far ordinal pseudo-structure without Citrus training labels, passed smoke and 250-step pilot gates, completed one fair 30-epoch run, selected `weights_25` by validation-only checkpoint selection, and reached test median-scaled `abs_rel=0.3840`, `a1=0.6539`. It is the current strongest Levinson label-free candidate, with promising mixed visual evidence rather than a clean solved visual story. Root code now remains active as the Snapshot 07 structure-aware branch.
17. 2026-05-20: Added the Levinson pre-presentation snapshot-folder audit now archived at `citrus_project/milestones/04_lightweight_vegetation_improvement/levinson/archive/research_process_notes/SNAPSHOT_FOLDER_AUDIT_BEFORE_PRESENTATION.md` and clarified stale historical wording in Snapshot 04/05/06, the Snapshot 05/06 checkpoint-selection note, and the pre-Snapshot07 audit so future readers do not confuse pre-Snapshot07 evidence with the current Snapshot 07 lead state.
18. 2026-05-20: Added `citrus_project/milestones/04_lightweight_vegetation_improvement/levinson/DOCUMENTATION_INDEX.md` as the AI-friendly Levinson documentation map before presentation preparation, and marked old planning/design notes as historical where needed.
19. 2026-05-20: Added `citrus_project/milestones/04_lightweight_vegetation_improvement/levinson/archive/research_process_notes/LEVINSON_DOCS_OPTIMIZATION_BEFORE_PRESENTATION.md` to record the Levinson Markdown inventory, current documentation hierarchy, historical/stale docs, redundant/confusing files, and presentation-readiness decision.
20. 2026-05-21: Reworked Slide 4 progress-presentation assets under `citrus_project/milestones/04_lightweight_vegetation_improvement/levinson/presentation_assets/progress_presentation/slide4_comparison/` into a research-evolution package focused on B0/Snapshot 00 through Snapshot 07, early pilot/gate failures, the teacher-anchor turning point, and Snapshot 07 as the current but visually mixed lead. No training, evaluation, model-code edits, dataset moves, checkpoint moves, or push were performed.
21. 2026-05-25: Ran two short post-hoc matched 250-step Slide 4 pilots for Snapshot 05 and Snapshot 06 using their teacher-anchor flags, evaluated first-100 validation, copied compact summaries into the Slide 4 asset folder, and removed the temporary run folders. Results: Snapshot 05 median-scaled `abs_rel=0.4779`, `a1=0.4224`; Snapshot 06 median-scaled `abs_rel=0.5523`, `a1=0.3696`. These are presentation-supporting matched gates and should be labeled post-hoc.
22. 2026-05-25: Created the repo/workspace tidy audit now archived at `citrus_project/milestones/04_lightweight_vegetation_improvement/levinson/archive/research_process_notes/REPO_TIDY_AUDIT.md` and made safe documentation-only navigation fixes. No files were deleted, moved, pushed, trained, or changed in model code; risky delete/archive candidates remain approval-only.
23. 2026-05-26: Moved Levinson root historical audit/planning notes into `citrus_project/milestones/04_lightweight_vegetation_improvement/levinson/archive/research_process_notes/` and deleted the ignored old `tmp_trainer_wiring_smoke/` smoke-output folder. No model code, datasets, weights, checkpoints, runs, local evidence, local results, Marvel files, or snapshot result files were changed.
24. 2026-05-28: Added Levinson `presentation_assets/` to shared `.gitignore` and clarified that presentation assets are not research source-of-truth. The paper-writing handoff should use the tracked source-of-truth docs, milestone READMEs, snapshot READMEs, research notes, and compact tracked summaries rather than ignored presentation-only folders.
25. 2026-06-03: Ran a verified literature deep-research pass for the next Levinson label-free improvement and recorded the ranked candidate shortlist at `citrus_project/milestones/04_lightweight_vegetation_improvement/levinson/LITERATURE_SHORTLIST.md` (self-supervised-pure options: feature-metric loss / FeatDepth, boundary-uncertainty mixtures / TSOB, surface-normal-planarity priors, multi-frame cost-volume as a training-only teacher; foundation-model distillation flagged as Marvel/hybrid, not label-free). Chosen direction (2026-06-03): pursue the self-supervised-pure options over roughly 2-3 weeks, starting this week with the feature-metric loss (FeatDepth-style) to show progress; boundary-uncertainty mixtures (TSOB) and the multi-frame training-only teacher are queued next. Foundation-model distillation stays out of Levinson's label-free scope. Also implemented an off-by-default `--teacher_texture_reliability_gate` (plus `_strength`/`_floor`) in root `options.py`/`trainer.py` as an untested incremental probe; not yet gated or snapshotted.
26. 2026-06-03: Implemented the chosen Snapshot 08 direction in root code (off by default): a FeatDepth-style feature-metric loss built ON TOP of the Snapshot 07 teacher-anchor stack. Added `networks/feature_net.py` (training-only image autoencoder), exported it, and added `--feature_metric_loss` plus `--feature_metric_weight/_warmup_steps/_channels`, `--feature_recon_weight`, `--feature_dis_weight`, `--feature_cvt_weight` in `options.py` with a `compute_feature_metric_losses` helper in `trainer.py`. The feature net is training-only; inference stays RGB-only Lite-Mono. Verified it is NOT a repeat of Snapshot 04's `feature_cross_view_consistency` (which used unshaped encoder features for cross-view consistency and hurt `a1`); the new loss adds the landscape regularization S04 lacked, and the gate will carry an explicit `a1` guard against the S04 failure pattern. CUDA one-step smoke (batch 12) PASSED with finite losses and finite new `feature_metric/*` diagnostics. Memory finding: full-resolution feature activations pushed peak VRAM to ~13.3 GiB (Snapshot 07 stack alone is ~7.7 GiB at batch 12), so a `--feature_metric_downsample` flag (default 2 = half-resolution) was added; half-res keeps the feature-metric signal (`fm_loss` ~0.163 vs ~0.166) and drops peak to ~8.95 GiB at batch 12 and ~7.46 GiB at batch 10. Decision: the laptop 3-epoch gate runs at batch 10 (fits 8 GB RTX 4060), the eventual full run uses batch 12 on the lab machine. Gate config = Snapshot 07 teacher-anchor stack (control) vs same + feature-metric (experiment), ImageNet-pretrain init, 3 epochs. Confirmed all Snapshot 07 sky/structure sub-params are option defaults, so both arms share an identical S07-faithful stack with only the non-default weights passed explicitly. Launched 2026-06-03, batch 10, seed 0. Gate completed 2026-06-03 ~17:00. Results (first-100 val, weights_2): control median-scaled `abs_rel=0.3150`, `a1=0.5152`; experiment `abs_rel=0.3318`, `a1=0.5169`. `a1` guard passed (no tank), but `abs_rel` worsened by 0.0168. Verdict: weak/mixed — `a1` held but `abs_rel` went backwards, a weaker gate signal than S05/S07 pilots which improved both. Decision: do NOT send to lab yet. Feature-metric code stays in the codebase (off by default) but is not promoted to a snapshot. Next direction: TSOB (boundary-uncertainty mixture distributions, BMVC 2025) — different mechanism (output representation, not input signal), targets same blob/boundary problem. Results saved at `runs/s08_gate_logs/gate_eval_results.md`. TSOB implementation completed 2026-06-03: added `networks/mixture_head.py`, `--tsob_mixture_loss` flags in `options.py`, `compute_tsob_mixture_loss` in `trainer.py`, one-line `("disp_feat", 0)` exposure in `depth_decoder.py`. CUDA smoke passed (batch 12 = 8.8 GB, batch 10 = 7.3 GB). 3-epoch gate completed 2026-06-03. First-100 val (weights_2): control `abs_rel=0.3411`, `a1=0.5155`; experiment `abs_rel=0.3234`, `a1=0.5805`. **Correction after Opus re-verification on FULL 560-sample val:** the first-100 "both improved" verdict was a small-sample artifact (two identical control runs differed by 0.026 in abs_rel but <0.001 in a1, so abs_rel is noisy at 100 samples). Full-val (weights_2): control `abs_rel=0.4373`, `a1=0.4979`; experiment `abs_rel=0.4558`, `a1=0.5393`. Real verdict: **TRADE-OFF, not a clean win** — a1/a2/a3 all improve (+0.041/+0.020/+0.013, de-blobbing works as intended) but abs_rel/sq_rel/rmse worsen (sharper-but-overconfident, matches TSOB paper's own caveat). It gives up S07's "matches original Lite-Mono abs_rel" property to push threshold accuracy. Decision PENDING re-evaluation: full run is cheap (one command on idle lab machine) and the abs_rel cost may shrink over 30 epochs (3 is very early), but success must be judged on a1 + VISUAL de-blobbing, not metrics alone — not on the discredited "both improved" claim. Methodology lesson: evaluate gates on full val (560), not first-100; use repeated controls as a noise estimate.
27. 2026-06-03: Documentation-only doc-layering cleanup of the top-level `AGENTS.md` Milestone 4 / Levinson coverage. Trimmed the per-snapshot duplication (the seven dedicated gate/snapshot sub-sections, the plain-baseline-planning block, Document Roles items 10-15, and Next Actions items) down to pointer + current-lead altitude; all trimmed facts were verified to already live in the Levinson subfolder docs. Moved the Milestone 4 change-log entries (formerly `AGENTS.md` items 13-38) into this `M4_CHANGE_LOG.md`. Preserved the two facts that existed only in `AGENTS.md` into `snapshots/00_plain_citrus_baseline/README.md` (the B0 `weights_15` mid-run probe and the discarded checkpoint-sweep note) and rounded out the B0 recipe there. Registered this log in `DOCUMENTATION_INDEX.md`. No code, runs, checkpoints, datasets, results, or evidence files were changed or deleted.
28. 2026-06-04: Lab machine is broken/unavailable, so the full TSOB run was moved to the laptop. After Opus re-verification confirmed the TSOB 3-epoch gate is a TRADE-OFF (a1/threshold up, abs_rel up/worse), the decision was to run the full 30-epoch TSOB AS-IS (no pre-tuning) rather than tune the known overconfidence knob first — rationale: only a converged run produces depth maps we can VISUALLY judge for de-blobbing (the real goal), and metrics already showed the trade-off. Launched full run on the laptop at batch 12 (paper setting, matching B0/S05/S07 for fair comparison; ~8.8 GiB peak spills slightly into shared memory so it runs a bit slower, but recipe-matching was prioritized over speed), seed 0, ImageNet pretrain, S07 stack + `--tsob_mixture_loss --tsob_weight 0.1 --tsob_warmup_steps 200`. Run folder `runs/s09_tsob_full_b12_30ep_seed0/`, logs `runs/s09_tsob_full_logs/`. Awaiting completion (~10-15 h), then validation-only checkpoint selection + full val/test eval + visual de-blobbing inspection; tune the overconfidence (smaller hypothesis offset / gentler entropy / lower weight) only if the pictures earn it.
29. 2026-06-05: TSOB full 30-epoch run (batch 12, laptop) completed. Validation-only checkpoint selection (B0-close-a1 rule) chose weights_24: val median abs_rel=0.4656, a1=0.5916; TEST median abs_rel=0.4331, a1=0.6492. Compared to Snapshot 07 (test abs_rel=0.3840, a1=0.6539) and original Lite-Mono (test abs_rel=0.3836, a1=0.4989): TSOB is WORSE than S07 on BOTH test metrics — clearly worse abs_rel (0.4331 vs 0.3840) and marginally worse a1 (0.6492 vs 0.6539). Visual check (Original vs S07 vs TSOB depth panels at runs/s09_tsob_full_logs/visuals/compare_original_s07_tsob.png) confirms NO visible de-blobbing: all three produce smooth ground-plane gradients and none crisply resolves side vegetation. FINAL VERDICT: TSOB (Snapshot 09) is a NEGATIVE result — neither numerically nor visually better than Snapshot 07. TSOB code (mixture head + loss) stays in the codebase off-by-default but is NOT promoted to a kept snapshot. Lesson reinforced (also from S08 feature-metric): aggressive boundary-sharpening backfires; pivot to gentle teacher-quality improvement. Next direction chosen: EMA mean-teacher in-domain self-distillation with consistency-filtered pseudo-labels (Round 2 literature shortlist #1) — replaces the fixed out-of-domain urban teacher with an adaptive self-teacher, aimed at abs_rel without trading off a1.
30. 2026-06-05: Ran a 20-agent research + design + adversarial-review workflow to engineer the next method. Output: Snapshot 10 DESIGN (design only, not built) = EMA in-domain self-teacher with two INDEPENDENT consistency filters (GC geometric min-reprojection, reused free from photo_error; DC flip-consistency of the EMA teacher) + a scale-and-shift-aware (Eigen SI-log) metric distillation term, on top of the S07 stack. DESIGN_NOTE at snapshots/10_ema_self_teacher/DESIGN_NOTE.md. KEY INSIGHT the reviewers proved: S07's purely normalized-log structure loss can only TIE abs_rel because median-scaling at eval divides out exactly that z-scored structure; S10 adds an SI-log term on metric log-depth that supervises the relative magnitude abs_rel actually rewards. Reviewers REJECTED the multi-frame cost-volume teacher (won't fit 8GB batch 12; AMP/feature-indexing blockers) and caught 4 bugs in naive versions (resume self.step desync, normalized-can't-move-abs_rel, DC-filter color-aug degeneracy, loss divided by num_scales). All off-by-default; inference unchanged single-image RGB. Expected full run ~24-28h (extra no-grad forwards push step time +30-50%). #1 risk: canopy keep-ratio collapse (GC rejects hard foliage -> trains only easy pixels -> ties S07); mandatory canopy keep-ratio logging + a cheap 3-epoch pre-gate (augmentation SI self-distillation) before the 24h commit. Awaiting user OK to build.
31. 2026-06-05: Implemented Snapshot 10 (EMA in-domain self-teacher) in root (off by default): `import copy`; a `self.ema_models` dict built by deepcopy of the student encoder+depth after init (frozen, kept OUT of self.models); auto warmup start_step (~5 epochs); `update_ema()` called after each optimizer step; `compute_ema_distill_loss()` (scale-and-shift-aware SI-log term + normalized structure anchor, gated by GC min-reprojection ∧ DC flip-consistency masks, adaptive τ·mean thresholds, canopy keep-ratio logging, starvation guard); EMA + step/epoch persistence in save_model (resume fix). All `--ema_*` flags off by default; no-flag runs are byte-for-byte S07; compiles clean. CUDA one-step smoke (batch 12, S07 stack + EMA forced active) PASSED: total loss 0.274 finite, peak VRAM 8244 MiB (~tiny shared-mem sliver, batch 12 holds), EMA updates, ema_distill diagnostics finite. NOTE: smoke keep_ratio≈0.93 (filters near-inert) is expected on an untrained scratch model; the real test is whether keep_ratio drops into 0.2-0.8 once trained — the #1 thing to watch at epoch ~10. Tested trainer.py/options.py copied into snapshots/10_ema_self_teacher/code/; diff vs S09 baseline in patches/. Updated ACTIVE_ROOT_CODE_STATE.md. Next: full 30-epoch run (same S07 recipe + EMA), watch-and-bail at epoch ~10.
32. 2026-06-09/10: Snapshot 10 first full run reached epoch 12 then was killed (Claude session/background process closed). weights_11 (12 epochs) evaluated on GPU, median-scaled: TEST abs_rel=0.3339, a1=0.6045; VAL abs_rel=0.3764, a1=0.5503. THIS IS A BREAKTHROUGH SIGNAL: at only 12/30 epochs S10 already BEATS original Lite-Mono (test abs_rel 0.3836) AND Snapshot 07 (test abs_rel 0.3840) on median-scaled abs_rel by ~0.05 — the first M4 method to BEAT (not tie) the original on abs_rel. a1 (0.6045) still trails S07 (0.6539) but was climbing (val a1 0.5463@ep11 -> 0.5503@ep12) and the run was not finished. The scale-and-shift-aware SI-log distillation term (the workflow's key fix) worked as designed. Partial proof saved at snapshots/10_ema_self_teacher/results/partial_epoch12_eval.md. Lesson: closing the Claude session kills laptop background runs — either keep the session open or run training in a standalone terminal. Restarted the clean 30-epoch run 2026-06-10 (same config) to get the finished, fair, a1-converged result; needs full val/test + checkpoint selection at the end.
33. 2026-06-10: Snapshot 10 (EMA in-domain self-teacher) full 30-epoch run COMPLETED and is a WIN. Validation-only checkpoint selection: the automatic B0-close-a1 rule fell back to an early artifact (weights_2, scale-ratio 6.2, pre-EMA, not converged) because no S10 checkpoint reaches B0 val a1>=0.59 (S10 sits ~0.57 on val); val abs_rel decreases smoothly to the final epoch, so the defensible pick is the FINAL converged checkpoint weights_29. weights_29 TEST median-scaled: abs_rel=0.3080, a1=0.6258 (val abs_rel=0.3453, a1=0.5712). VERDICT: vs ORIGINAL Lite-Mono (test abs_rel 0.3836, a1 0.4989) a CLEAN WIN on BOTH — abs_rel ~20% better, a1 ~25% better — the FIRST Milestone 4 method to BEAT (not tie) the original on abs_rel. vs Snapshot 07 (test abs_rel 0.3840, a1 0.6539): big abs_rel win (~20%) for a small a1 give-back (-0.028) — a good trade, opposite of the S08/S09 failures. New strongest Levinson label-free result. Inference unchanged (single-image RGB-only); training label-free (EMA self-teacher). Results: snapshots/10_ema_self_teacher/results/final_result.md + validation_sweep.csv. Honest remaining limits: a1 just under S07; sky/far confusion + canopy over-smoothing not visually solved (S10 targets accuracy, not appearance).
34. 2026-06-11: Preserved winning inference checkpoints (encoder.pth+depth.pth) into snapshot folders before any cleanup: B0 (snapshot 00 checkpoint/), S07 weights_25 (snapshot 07 checkpoint/), S10 weights_29 (snapshot 10 checkpoint/) — committed. Then a CONSERVATIVE runs/ cleanup (user-approved): deleted 40 disposable folders (all *smoke* runs, all 250-step gate/pilot/ablation runs incl. vg_tcv S04 ablations + their stray .log files, and the 3-epoch S08/S09 gate runs), freeing ~11 GB (47->37 GB). Each deleted run's metrics/conclusions were already recorded in its snapshot. KEPT: all 6 full 30-epoch runs (B0/S05/S06/S07/S09-TSOB/S10) and the 4 small *_logs folders (checkpoint-selection eval summaries). runs/ is gitignored (local only). Further space (the kept full runs, e.g. the redundant B0/S07/S10 runs whose weights are already preserved, and the negative TSOB full) can be reclaimed later if desired.
35. 2026-06-11 (fresh-session critical review of Snapshot 10): verification-only session, no training. (a) Independently re-ran the full TEST eval on the preserved S10 checkpoint (weights_29 copy) — reproduced abs_rel=0.3080, a1=0.6258, a2=0.8118, a3=0.9005 exactly; durable summary saved to `snapshots/10_ema_self_teacher/results/weights29_test_reverify/` (the headline test numbers previously had no on-disk machine-generated artifact; the run-log test folder only held the auto-rule weights_2 eval). (b) Closed the "keep-ratio never directly verified" gap: wrote a dependency-free TB event parser (`snapshots/10_ema_self_teacher/diagnostics/read_tb_scalars.py`; the env has the tensorboardX writer but no TB reader) and extracted all `ema_distill/*` scalars (`diagnostics/ema_distill_scalars_train.csv`). FINDING: the DC∧GC filters were NEAR-INERT all run — keep-ratio 0.87–0.92 overall AND in the canopy band, never inside the 0.2–0.8 design target; the abs_rel win therefore came from near-dense EMA SI-log self-distillation, not selective gating (τ·mean with τ=3.0 keeps ~88% of pixels on right-skewed errors). Filter tightening/ablation identified as the top next lever; gating cannot be credited in the paper without an ablation. (c) Confirmed the a1 deficit is a method property (val a1 plateaus ≈0.57 across all converged checkpoints; max 0.5743 at weights_19), not a selection artifact; the weights_2 auto-rule fallback confirmed pathological (test a1 0.5715 despite same abs_rel). (d) Paired per-frame test comparison vs S07 weights_25: abs_rel win on 62.4% of frames, large wins clustered in S07's worst time-blocks; a1 give-back broad (68% of frames). (e) Verified root trainer.py/options.py byte-identical to the S10 snapshot `code/` copies and the run's opt.json matches the DESIGN_NOTE defaults. Findings recorded in `snapshots/10_ema_self_teacher/results/final_result.md` (post-hoc verification section).
36. 2026-06-11: Test-set failure autopsy (no training) recorded at `snapshots/10_ema_self_teacher/diagnostics/failure_autopsy_2026-06-11.md`. Key findings: all 40 worst S10 test frames are shared failures (also bad for S07 AND original — S10 added zero new catastrophic failures); worst frames are wide-open clearings / row-end scenes; the measured abs_rel error concentrates in NEAR-ground frames (gt_median 1.3–1.8 m bucket: mean abs_rel 0.455 vs 0.211 for the 3.0–3.3 m bucket), i.e. mis-sloped bare-ground geometry where labels are nearest/densest — while the visual blob problem lives in far canopy/sky largely outside the labeled region. Evidence newly points at the previously-deprioritized surface-normal/planarity prior (literature shortlist Round 1 A.3) as the abs_rel-aligned gentle structural lever. Same-day panel rendering (Original/S07/S10 vs masked LiDAR label, 9 frames, script + outputs under the S10 snapshot diagnostics//local_evidence) then CONFIRMED a stronger reading: all three models predict near-identical smooth bottom-to-top ground-ramp disparity with only faint lateral tree structure; per-frame score is governed by how well the true scene matches a vertical ramp (corridors fit, clearings do not). S10's win is a better-CALIBRATED ramp, not new object structure — explaining the metric-vs-visual divergence across S05-S10 — and the planarity-prior idea was correspondingly DOWNGRADED (the model is already over-smooth); the open gap is lateral depth structure. Same-day ground-vs-vegetation error split (excess-green pixel classification, full 407-frame test set, original + S10): labeled pixels are ~84% ground / ~16% vegetation, so the metric is mostly ground accuracy; S10's gain came mostly from ground pixels (0.382→0.299, −22%) vs vegetation (0.368→0.334, −9%), quantitatively confirming the ramp-calibration reading; for S10 vegetation is now the weaker region but with limited metric mass, while the largest remaining metric mass is clearing-frame ground geometry. Method implication recorded: the failure is an image-row→depth shortcut, and resizing-crop/split-permute augmentation self-distillation (BDEdepth, Round 2 shortlist #3) is now the best evidence-matched next candidate (breaks position shortcuts, composes with the S10 stack). Full numbers in the autopsy note.
37. 2026-06-11: Wrote the ONE-SHOT Snapshot 11 plan at `snapshots/11_resizing_crop_self_distillation/DESIGN_NOTE.md` (plan only — not built, not run, awaiting explicit user OK; ~1.5 weeks remain). Method: resizing-crop self-distillation on top of the frozen S10 stack — cropped/zoomed student view distilled toward the crop of the EMA teacher's existing clean-view prediction, reusing S10's SI-log + normalized-structure losses verbatim (both invariant to the crop-zoom scale ambiguity) and the DC∧GC mask; one new GRAD forward per step, mandatory memory smoke with a sub-batch/batch-10 fallback ladder. Gate: 8-epoch run, full-val eval at epochs 5/6/7 against S10's own validation_sweep.csv as a zero-cost same-seed control; predefined PASS (abs_rel ≥0.005 better at epochs 6 AND 7, a1 within 0.005) / KILL (anything else, mixed included ⇒ ship S10 as the paper result, no method-hopping). User constraints recorded: filter ablation deferred to paper-polish; previous snapshots S00–S10 not to be modified; no training without explicit OK.
38. 2026-06-11: Ran the S11 pre-build mechanism probe (inference-only, ~2 min, no training): trained S10 contradicts itself between full and 0.7-crop views by a mean 0.118 SI-log scale-free disagreement (~12%) across all 407 test frames — the position shortcut is real and the planned crop-consistency loss has a large trainable signal (necessary condition PASSED; ~0.01 would have killed S11 pre-build). However per-frame correlation between self-inconsistency and abs_rel is weak (pearson 0.19, top-40 overlap 11/40), so the metric benefit is expected as aggregate regularization, not guaranteed — the 8-epoch gate remains mandatory before the full run. Script + per-frame CSV in `snapshots/11_resizing_crop_self_distillation/diagnostics/`; probe section added to the S11 DESIGN_NOTE.
39. 2026-06-11: Implemented Snapshot 11 in root code (off by default; user approved coding, NOT training): `--crop_self_distillation` + `--crop_distill_weight/_warmup_steps`, `--crop_scale_min/max`, `--crop_struct_weight`, `--crop_distill_subbatch`, `--crop_view_downsample` in `options.py`; `compute_crop_distill_loss` in `trainer.py` (per-sample affine-grid crops via one batched grid_sample; SI-log term with PER-SAMPLE offset removal — the per-sample crop-zoom scale demands it, unlike S10's batch-wide offset; normalized-structure anchor; DC∧GC mask cropped along; starvation guard) plus a crop-flag-gated cache of the EMA teacher disparity/mask in `compute_ema_distill_loss`. Flag-off behavior verified unchanged (insertions-only diff vs the S10 code copy; compile clean). Tested code copied to `snapshots/11_resizing_crop_self_distillation/code/`, diffs in `patches/`, `ACTIVE_ROOT_CODE_STATE.md` updated. CUDA smoke ladder (3 full optimizer steps each, batch 12, S10 opt.json, EMA forced active; all losses finite in every config) measured peak alloc/reserved: full-batch full-res 12.2/12.8 GiB (heavy WDDM spill, not viable); sb6 10.5/10.9; sb3 9.6/10.0; full-batch half-res 9.6/10.0; **sb6 half-res 9.1/9.7 — CHOSEN** (closest to the proven S09 ~8.8 GiB regime); sb3 half-res 8.9/9.6 (fallback, fixed overhead dominates; half-res ladder measured 2026-06-13). Recipe preserved: batch 12 + full resolution for all original losses; levers shrink only the added crop lesson (S08 half-res precedent). Gate bail rule: crop-active epoch >~90 min → relaunch with the fallback. AWAITING explicit user OK for the 8-epoch gate. Same-day flow improvement (user's suggestion): the separate 8-epoch gate + restart was replaced by a SINGLE 30-epoch launch — identical config/seed makes the first 8 epochs the gate; weights_5/6/7 are CPU-evaluated mid-run against S10's validation sweep (GPU undisturbed; S10 epoch-10/18 CPU-eval precedent) with predefined kill-or-continue rules; saves ~6 h in the pass branch. Killed-S11 salvage is possible post-deadline or on a >13 GiB machine; S10 remains the paper result in the kill branch.
40. 2026-06-13: Ran the Round 3 verified deep-research pass (102 agents, 20 sources, 100 claims, top 25 adversarially 3-vote verified: 23 confirmed / 2 refuted), targeted at the failure-autopsy diagnoses; results recorded as the Round 3 section of `LITERATURE_SHORTLIST.md`. Headlines: (a) our position-shortcut diagnosis is CONFIRMED as a known peer-reviewed property of small photometric-self-supervised CNNs (van Dijk ICCV 2019; EPCDepth ICCV 2021; DaCCN ICCV 2023) — large supervised ViTs escape it, our regime does not; (b) the clearing failures match the published partial-pitch-compensation mechanism; (c) the canopy-blob problem and the S08/S09 sharpening failures now have a principled explanation (object detection keyed to ground-contact edges/shadows, which canopies lack) — cue-diversifying augmentation, not sharper losses, is the indicated lever; (d) S11's mechanism family is validated (AugUndo ECCV 2024 — tested on Lite-Mono itself — strictly contains S11's transforms; RA-Depth ECCV 2022 is the published sibling); (e) post-S11 queue recorded: Data Grafting in the distillation branch, then a label-free PDA pitch/roll-equivariance re-derivation (unpublished = novelty); (f) verified gaps: no orchard/vegetation-validated self-sup depth work, no published canopy fix — unclaimed paper territory. Refuted (do not cite): FUMET as a clean label-free fit; DaCCN per-module ablation numbers.
41. 2026-06-13: LAUNCHED the Snapshot 11 single 30-epoch run with the user's explicit GO: `s11_crop_distill_b12_30ep_seed0`, detached standalone python process (survives session close — lesson from the killed first S10 run), exact S10 recipe (batch 12, seed 0, ImageNet pretrain, S07 stack + EMA flags, command reconstructed by diffing S10's opt.json against option defaults) plus `--crop_self_distillation --crop_distill_subbatch 6 --crop_view_downsample 2`. Command archived at `snapshots/11_resizing_crop_self_distillation/commands/full_run_launch_command.txt`; logs at `runs/s11_crop_logs/`. Watch-and-bail gate: CPU-evaluate weights_5/6/7 on full val (~6 h in) against S10's validation_sweep.csv; kill/continue rules in the S11 DESIGN_NOTE Section 4; runtime bail if a crop-active epoch exceeds ~90 min.

42. 2026-06-14: S11 launch 1 KILLED at epoch 5 and RELAUNCHED as r2 after a found-and-fixed implementation bug. The run was on pace (epochs 0-4 ~32 min, crop-active epoch 5 = 44 min, under the 90-min bail), but the epoch-5 full-val CPU gate eval showed an artifact-class failure (median-scaled abs_rel 0.5774, a1 0.3419, scale-ratio 12.2 vs S10 epoch-5 0.4688/0.5524/~3.4). Root cause confirmed in code: Lite-Mono's encoder uses nn.BatchNorm2d, and the crop branch's TRAIN-MODE student forward on zoomed half-res sub-batches polluted the BN running statistics used at eval (train losses healthy, eval drifted - invisible to the 3-step smoke, impossible in S10 whose extra forwards used frozen eval-mode EMA copies). Fix: freeze BatchNorm2d modules during the crop forward only (eval-mode running-stats affine; gradients flow; main forward updates stats normally). Patch re-smoked (losses finite, memory unchanged 9.1/9.7 GiB), snapshot code/patches/commands re-synced, poisoned run folder kept as evidence. Classed as a BUG failure, not a method verdict - gate rules and the S10-sweep comparison unchanged. R2 relaunched fresh from epoch 0 (same seed/recipe; full epoch-matched comparability preserved), detached process, logs runs/s11_crop_logs/train_r2*.log, run folder runs/s11_crop_distill_b12_30ep_seed0_r2/. Post-mortem in the S11 DESIGN_NOTE.

43. 2026-06-14: S11 r2 KILLED BY THE GATE RULE - Snapshot 11 is a NEGATIVE RESULT under this integration. R2 reproduced launch 1 almost exactly (the BN hypothesis of entry 42 was WRONG as the main cause - the BN freeze changed nothing, proving determinism), and the epoch 5/6 full-val evals were frozen bit-identical (abs_rel 0.575, a1 0.343, scale-ratio 12.1) because the model collapsed to the DEGENERATE FLAT-DEPTH solution: predicted median depth ~0.20 m constant across all 564 val frames vs true 1.0-4.6 m. Mechanism: both S11 crop terms are scale-free (per-sample SI-log offset removal + normalized structure), which a constant output satisfies trivially; the fast EMA teacher follows the student into collapse; the DC filter passes a flat (perfectly flip-consistent) teacher; and the photometric loss resistance is neutralized by the classic zero-translation/automask degeneracy. Published BDEdepth/RA-Depth avoid this via METRIC-ANCHORED zoom-rescaled targets applied from epoch 0. Candidate fix recorded in the S11 DESIGN_NOTE (metric-anchored target = crop(teacher_disp)/scale_factor, no offset removal, early activation) for post-deadline revisit; NOT attempted under the ~1-week deadline per the one-shot rule. Total cost of S11: ~8 h GPU across two launches; both run folders kept as evidence; gate evals at runs/s11_crop_logs/gate_eval*/. VERDICT: Snapshot 10 weights_29 ships as the Levinson label-free paper result; remaining time pivots to paper-strengthening (S10 filter ablation, second-sequence generalization, tables/figures) per the written plan.

44. 2026-06-14: Drafted the publishable paper v1 in a new `paper/` folder (single `main.tex`, IEEEtran two-column, ~536 lines; `references.bib`; `figures/` with 8 figures; `make_figures.py`; `OUTLINE.md`; `README.md`). Hero = Snapshot 10; S01-S11 framed as honest ablations/negatives; the ground-ramp position-shortcut diagnosis is a contribution. Sections: intro, related work, dataset/protocol, method (EMA self-teacher math), lineage/ablations, results (main table + per-frame), failure diagnosis, negative results (S08/S09/S11), limitations, future work, conclusion. All numbers pulled from on-disk eval artifacts (verified table: Original 0.3836/0.4989, B0 0.4889/0.6582, S05 0.3947/0.6476, S07 0.3840/0.6539, S09 0.4331/0.6492, S10 0.3080/0.6258). Honesty guardrails enforced throughout (near-inert gates -> gain credited to dense distillation not gating; S10 beats ORIGINAL on both but B0/S07 have higher d1, explicitly stated; metrics-not-visuals; single-domain; eval-only GT). Self-review passes done: all 18 cite-keys resolve, all 7 referenced figure files exist, all refs/labels matched, no unescaped underscores. No local LaTeX toolchain -> not compiled here; compile on Overleaf. NEEDS A HUMAN: author block, Fig.1 method schematic (text placeholder), and verification of `% UNVERIFIED` references (esp. the CitrusFarm dataset citation). Optional non-blocking strengtheners noted: gating ablation, second-sequence generalization.
